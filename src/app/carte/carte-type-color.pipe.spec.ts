import { CarteTypeColorPipe } from './carte-type-color.pipe';

describe('CarteTypeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new CarteTypeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
