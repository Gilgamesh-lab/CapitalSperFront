import { carteCampColorPipe } from './carte-camp-color.pipe';

describe('carteCampColorPipe', () => {
  it('create an instance', () => {
    const pipe = new carteCampColorPipe();
    expect(pipe).toBeTruthy();
  });
});
