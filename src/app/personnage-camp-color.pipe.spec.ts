import { PersonnageCampColorPipe } from './personnage-camp-color.pipe';

describe('PersonnageCampColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PersonnageCampColorPipe();
    expect(pipe).toBeTruthy();
  });
});
