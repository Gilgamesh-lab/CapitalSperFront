import { PersonnagePouvoirColorPipe } from './personnage-pouvoir-color.pipe';

describe('PersonnagePouvoirColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PersonnagePouvoirColorPipe();
    expect(pipe).toBeTruthy();
  });
});
